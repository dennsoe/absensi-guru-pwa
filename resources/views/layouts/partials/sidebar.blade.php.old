<aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
        <img src="{{ asset('assets/images/logonekas.png') }}" alt="Logo NEKAS" class="sidebar-logo">
        <div class="sidebar-title">
            <h5>SIAG NEKAS</h5>
            <small>SMK Negeri Kasomalang</small>
        </div>
    </div>

    <div class="sidebar-user">
        <div class="d-flex align-items-center">
            <div class="avatar">
                <i class="bi bi-person-circle"></i>
            </div>
            <div class="user-info">
                <div class="user-name">{{ auth()->user()->guru->nama ?? auth()->user()->username }}</div>
                <div class="user-role">
                    @switch(auth()->user()->role)
                        @case('admin')
                            <span class="badge bg-danger">Admin</span>
                        @break

                        @case('guru')
                            <span class="badge bg-primary">Guru</span>
                        @break

                        @case('guru_piket')
                            <span class="badge bg-info">Guru Piket</span>
                        @break

                        @case('kepala_sekolah')
                            <span class="badge bg-success">Kepala Sekolah</span>
                        @break

                        @case('kurikulum')
                            <span class="badge bg-warning">Kurikulum</span>
                        @break

                        @case('ketua_kelas')
                            <span class="badge bg-secondary">Ketua Kelas</span>
                        @break
                    @endswitch
                </div>
            </div>
        </div>
    </div>

    <nav class="sidebar-menu">
        <ul class="nav flex-column">
            <!-- Dashboard -->
            <li class="nav-item">
                <a href="{{ route('dashboard') }}"
                    class="nav-link {{ request()->routeIs('dashboard') || request()->routeIs('*.dashboard') ? 'active' : '' }}">
                    <i class="bi bi-speedometer2"></i>
                    <span>Dashboard</span>
                </a>
            </li>

            @if (auth()->user()->role === 'admin')
                <!-- Admin Menu -->
                <li class="nav-header">MANAJEMEN</li>
                <li class="nav-item">
                    <a href="{{ route('admin.users') }}"
                        class="nav-link {{ request()->routeIs('admin.users*') ? 'active' : '' }}">
                        <i class="bi bi-people"></i>
                        <span>Kelola User</span>
                    </a>
                </li>
            @endif

            @if (in_array(auth()->user()->role, ['guru', 'ketua_kelas']))
                <!-- Guru Menu -->
                <li class="nav-header">ABSENSI SAYA</li>
                <li class="nav-item">
                    <a href="{{ route('guru.absensi.riwayat') }}"
                        class="nav-link {{ request()->routeIs('guru.absensi*') ? 'active' : '' }}">
                        <i class="bi bi-clock-history"></i>
                        <span>Riwayat Absensi</span>
                    </a>
                </li>
            @endif

            @if (auth()->user()->role === 'guru_piket')
                <!-- Guru Piket Menu -->
                <li class="nav-header">MONITORING</li>
                <li class="nav-item">
                    <a href="{{ route('piket.monitoring') }}"
                        class="nav-link {{ request()->routeIs('piket.monitoring') ? 'active' : '' }}">
                        <i class="bi bi-tv"></i>
                        <span>Monitor Real-Time</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ route('piket.absensi-manual') }}"
                        class="nav-link {{ request()->routeIs('piket.absensi-manual*') ? 'active' : '' }}">
                        <i class="bi bi-pencil-square"></i>
                        <span>Input Manual</span>
                    </a>
                </li>
            @endif

            @if (auth()->user()->role === 'kepala_sekolah')
                <!-- Kepala Sekolah Menu -->
                <li class="nav-header">APPROVAL</li>
                <li class="nav-item">
                    <a href="{{ route('kepsek.izin-cuti') }}"
                        class="nav-link {{ request()->routeIs('kepsek.izin-cuti*') ? 'active' : '' }}">
                        <i class="bi bi-file-earmark-check"></i>
                        <span>Izin & Cuti</span>
                        @php
                            $pendingCount = \App\Models\IzinCuti::where('status', 'pending')->count();
                        @endphp
                        @if ($pendingCount > 0)
                            <span class="badge bg-danger ms-auto">{{ $pendingCount }}</span>
                        @endif
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ route('kepsek.laporan.kedisiplinan') }}"
                        class="nav-link {{ request()->routeIs('kepsek.laporan*') ? 'active' : '' }}">
                        <i class="bi bi-file-earmark-bar-graph"></i>
                        <span>Laporan Kedisiplinan</span>
                    </a>
                </li>
            @endif

            @if (auth()->user()->role === 'kurikulum')
                <!-- Kurikulum Menu -->
                <li class="nav-header">AKADEMIK</li>
                <li class="nav-item">
                    <a href="{{ route('kurikulum.jadwal') }}"
                        class="nav-link {{ request()->routeIs('kurikulum.jadwal*') ? 'active' : '' }}">
                        <i class="bi bi-calendar3"></i>
                        <span>Jadwal Mengajar</span>
                    </a>
                </li>
            @endif

            <!-- Common Menu -->
            <li class="nav-header">ABSENSI</li>
            <li class="nav-item">
                <a href="{{ route('absensi.scan-qr') }}"
                    class="nav-link {{ request()->routeIs('absensi.scan-qr') ? 'active' : '' }}">
                    <i class="bi bi-qr-code-scan"></i>
                    <span>Scan QR Code</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ route('absensi.selfie') }}"
                    class="nav-link {{ request()->routeIs('absensi.selfie') ? 'active' : '' }}">
                    <i class="bi bi-camera"></i>
                    <span>Absensi Selfie</span>
                </a>
            </li>

            <li class="nav-header">JADWAL</li>
            <li class="nav-item">
                <a href="{{ route('jadwal.hari-ini') }}"
                    class="nav-link {{ request()->routeIs('jadwal.hari-ini') ? 'active' : '' }}">
                    <i class="bi bi-calendar-day"></i>
                    <span>Jadwal Hari Ini</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ route('jadwal.per-kelas') }}"
                    class="nav-link {{ request()->routeIs('jadwal.per-kelas') ? 'active' : '' }}">
                    <i class="bi bi-building"></i>
                    <span>Jadwal Per Kelas</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ route('jadwal.per-guru') }}"
                    class="nav-link {{ request()->routeIs('jadwal.per-guru') ? 'active' : '' }}">
                    <i class="bi bi-person-badge"></i>
                    <span>Jadwal Per Guru</span>
                </a>
            </li>

            @if (in_array(auth()->user()->role, ['admin', 'kepala_sekolah', 'kurikulum']))
                <li class="nav-header">LAPORAN</li>
                <li class="nav-item">
                    <a href="{{ route('laporan.index') }}"
                        class="nav-link {{ request()->routeIs('laporan.*') ? 'active' : '' }}">
                        <i class="bi bi-graph-up"></i>
                        <span>Rekap Absensi</span>
                    </a>
                </li>
            @endif
        </ul>
    </nav>

    <div class="sidebar-footer">
        <form action="{{ route('logout') }}" method="POST">
            @csrf
            <button type="submit" class="btn btn-outline-danger btn-sm w-100">
                <i class="bi bi-box-arrow-right"></i>
                Logout
            </button>
        </form>
    </div>
</aside>

<style>
    .sidebar {
        position: fixed;
        top: 0;
        left: 0;
        height: 100vh;
        width: var(--sidebar-width);
        background: linear-gradient(180deg, #1e40af 0%, #1e3a8a 100%);
        color: white;
        overflow-y: auto;
        transition: transform 0.3s ease;
        z-index: 1000;
        display: flex;
        flex-direction: column;
    }

    .sidebar-header {
        padding: 1.5rem;
        text-align: center;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .sidebar-logo {
        width: 60px;
        height: 60px;
        object-fit: contain;
        margin-bottom: 0.75rem;
    }

    .sidebar-title h5 {
        font-weight: 700;
        font-size: 1.1rem;
        margin: 0;
        color: white;
    }

    .sidebar-title small {
        font-size: 0.8rem;
        color: rgba(255, 255, 255, 0.8);
    }

    .sidebar-user {
        padding: 1rem 1.5rem;
        background: rgba(0, 0, 0, 0.1);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-right: 0.75rem;
    }

    .avatar i {
        font-size: 1.5rem;
        color: white;
    }

    .user-info {
        flex: 1;
    }

    .user-name {
        font-weight: 600;
        font-size: 0.9rem;
        color: white;
        margin-bottom: 0.25rem;
    }

    .user-role .badge {
        font-size: 0.7rem;
        font-weight: 500;
    }

    .sidebar-menu {
        flex: 1;
        padding: 1rem 0;
        overflow-y: auto;
    }

    .sidebar-menu::-webkit-scrollbar {
        width: 4px;
    }

    .sidebar-menu::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.3);
        border-radius: 4px;
    }

    .nav-header {
        padding: 1rem 1.5rem 0.5rem;
        font-size: 0.7rem;
        font-weight: 600;
        color: rgba(255, 255, 255, 0.6);
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .sidebar-menu .nav-link {
        padding: 0.75rem 1.5rem;
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        display: flex;
        align-items: center;
        transition: all 0.2s ease;
        border-left: 3px solid transparent;
    }

    .sidebar-menu .nav-link:hover {
        background: rgba(255, 255, 255, 0.1);
        color: white;
        border-left-color: rgba(255, 255, 255, 0.5);
    }

    .sidebar-menu .nav-link.active {
        background: rgba(255, 255, 255, 0.15);
        color: white;
        border-left-color: white;
        font-weight: 600;
    }

    .sidebar-menu .nav-link i {
        font-size: 1.1rem;
        margin-right: 0.75rem;
        width: 20px;
        text-align: center;
    }

    .sidebar-menu .nav-link .badge {
        font-size: 0.65rem;
        padding: 0.25rem 0.5rem;
    }

    .sidebar-footer {
        padding: 1rem 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    @media (max-width: 768px) {
        .sidebar {
            transform: translateX(-100%);
        }

        .sidebar.active {
            transform: translateX(0);
        }
    }
</style>
